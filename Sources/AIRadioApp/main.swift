import Foundation
import AIRadioCore
import AIRadioInfra

// S0 スタブ。メニューバー UI と放送エンジンは後続スライス（S7 / S3〜）で実装する。
let clock: Clock = SystemClock()
print("ケイラボAIラジオ Mac版 — skeleton (S0)")
print("起動時刻: \(clock.now)")
print("放送エンジン・メニューバー UI は後続スライスで実装します。")
