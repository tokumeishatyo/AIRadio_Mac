# LLM（台本生成）設定。API キーは llm.local.yaml（gitignore 対象、サンプル参照）に置く。
# model は GET /v1beta/models で実在確認済み（2026-06-12）。
# 代替候補（同日確認済み・無料枠）: gemma-4-26b-a4b-it / gemini-flash-lite-latest
llm:
  provider: gemini
  model: "gemini-3.1-flash-lite"
  endpoint: "https://generativelanguage.googleapis.com/"
  temperature: 0.9
